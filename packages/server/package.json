{
  "name": "@chat-agent-relay/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "car-server": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@chat-agent-relay/pipeline": "workspace:*",
    "@chat-agent-relay/channel-slack": "workspace:*",
    "@chat-agent-relay/backend-openai": "workspace:*",
    "@chat-agent-relay/event-ledger": "workspace:*",
    "@chat-agent-relay/middleware": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^6.0.2"
  }
}
