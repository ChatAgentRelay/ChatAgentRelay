{
  "name": "@cap/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "cap-server": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@cap/pipeline": "workspace:*",
    "@cap/channel-slack": "workspace:*",
    "@cap/backend-openai": "workspace:*",
    "@cap/event-ledger": "workspace:*",
    "@cap/middleware": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.2"
  }
}
