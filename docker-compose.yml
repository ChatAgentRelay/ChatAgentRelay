services:
  cap-server:
    build: .
    ports:
      - "${CAP_API_PORT:-3000}:3000"
    env_file:
      - packages/server/.env
    volumes:
      - cap-data:/app/data
    environment:
      - CAP_SQLITE_PATH=/app/data/cap-ledger.db
    restart: unless-stopped

volumes:
  cap-data:
